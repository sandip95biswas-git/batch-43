13 14 20  24 27 28 

Part 1 AWS Ubuntu EC2 instance:
Launch a new EC2 instance
Ubuntu Server 20.04 LTS
Allowing essential port i.e 8501



Part-2 Application installation 
Step-1 update & Install Python 3.7+ and pip

sudo -i
sudo apt update && sudo apt install python3 python3-pip python3-venv -y

Step-2  Install Git  
sudo apt install git -y

Step-3 Install Poppler (for pdf2image)

sudo apt install poppler-utils -y

git clone https://github.com/CloudDevOpsHub/ATS-Multi-Cloud-AI-Project.git       (repos are modified>>in app.py file>> gemini API updated to 3.5-flash from 2.0-flash)
git clone https://github.com/CloudDevOpsHub/Application-Tracking-System.git     (repos are modified>>in app.py file>> gemini API updated to 3.5-flash from 2.0-flash)

cd to the cloned git file

Step-3 Set Up a Virtual Environment
 python3 -m venv venv
 source venv/bin/activate

Step-4 Install Project Dependencies
 pip install --upgrade pip
 pip install -r requirements.txt
 pip install google-generativeai


---------------------


AWS + GCP + AI ATS System  
Part 4 AI Gemini Setup 

Get Gemini API Key from Google Cloud Platform (GCP)
Go to google cloud console à Create project àsearch gemini apiàclick on gemini api --> manage -->Generative Language API->credentials --> create credentails -->get api
search credentials>> create new credentials>> API key >>selecting gemini API (if not enabled: enable the API)>> intregate this with your google service account (if not create the service account) (for free tier accounts) (due to the generative API deprication not using the API key "Gemini for G cloud API")
Part-5 Connect AWS ATS application with GCP Gemini AI
 
1.Add the API Key to Streamlit Secrets
mkdir -p .streamlit
cd .streamlit
vi secrets.toml

GOOGLE_API_KEY = "your-api-key-here"

2.Run the Streamlit App
    streamlit run app.py --server.port 8501 --server.enableCORS false






